# Retired comparison social card

The PNG in this directory is the former static comparison social card. It is retained for recovery, not served as current metadata. Its coverage counts are historical and must not be used as current product data.

The live route now generates a count-free social card in `app/prop-firm-challenges/opengraph-image.tsx`. Dataset expiry must not prevent page regeneration.
