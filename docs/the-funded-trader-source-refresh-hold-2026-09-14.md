# The Funded Trader source refresh hold — 2026-09-14

The Funded Trader's first-party homepage (`https://www.thefundedtraderprogram.com/`) was checked for a complete product recapture. A direct browser check on 2026-09-16 returned a first-party `Deployment Paused` page; cached search results expose an updated Knight/Royal/Dragon lineup and headline package values, but those snippets are not a live selector. Because the live product selector, account size, platform, add-ons and checkout total cannot be read together, the refreshed product ladder, prices and country rules cannot be verified from the firm's own site.

The existing seven-product capture remains dated 2026-07-27. Search-result snippets and third-party summaries are not sufficient evidence for a challenge data merge, so no product date or numeric field was changed. The cached snippets are retained only as a lead for the next direct capture, not as source data.

Next safe action: wait for the firm's public deployment to return, then capture the complete first-party product and eligibility pages in one pass. Keep the firm outside freshness-gated rankings until that capture is complete.

## Follow-up check — 2026-09-15

The indexed first-party homepage now renders a five-program comparison with headline `$100k` prices of `$578`, `$519` and `$489`, alongside maximum virtual allocations of `$400k`, `$300k` and `$2.5m`. The same page leaves the live selector's programme, platform, balance and add-on values as placeholders in the server-readable response. Those headline values cannot be safely mapped to the existing seven products or used to reconstruct the complete tier ladder, so the 2026-07-27 capture remains held.

## Follow-up check — 2026-09-16

The official configurator URL returned a first-party “Deployment Paused” page during a read-only browser check. No product selector, country gate or checkout total was available to capture. The seven structured products therefore remain dated 2026-07-27 and excluded from fresh comparisons until the public deployment is restored and a named-product capture is possible.
