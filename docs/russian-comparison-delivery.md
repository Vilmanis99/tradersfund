# Russian-first comparison delivery

Direction: turn the existing Russian section into a useful comparison service for Russian-speaking traders worldwide, with original editorial explanations supporting the tools. Preserve established URLs and the English site. Do not copy competitor content, review scores, affiliate codes or eligibility claims.

## Implemented in this release

- Shared server-side challenge projection for English and Russian; source age evaluated on page regeneration.
- Russian finder on the existing `/ru/luchshie-prop-firmy` URL: 14 captured programmes across FundedNext, Bright Funded, FundingPips and non-affiliate FTMO as of September 8, 2026.
- Exact account-size, phase, currency, budget and drawdown filtering. Currency-specific price ordering; no implicit FX conversion or commercial weighting.
- Three-product comparison with differences highlighted, shareable fragment URLs, missing-data labels, source dates, fee-floor caveats and country/KYC boundaries.
- Compact homepage entry preserving the two disclosed primary partners; no restored article wall or new mass-generated pages.
- Count-free generated English social card. Retired bitmap archived rather than used as current evidence. Expiring datasets must not throw during ISR and freeze cached comparisons.
- Russian homepage and ranking sitemap editorial dates updated; existing route/hreflang mappings retained.

## Verification

- `npm run test:finder`: projection parity, independence from partner flags, currency/budget boundaries, filtering, share-state round trips, malformed input, unknown fields and day-30/day-31 source expiry.
- `npm run check:finder-release [base-url]`: all 26 Russian URLs, HTTP 200, one H1, language, self-canonical, sitemap inclusion and changed-route lastmod; source-backed server-rendered finder rows; disclosure and country boundary; generated PNG dimensions.
- Production build: 509 pages; changed comparison routes have hourly regeneration.
- Browser: homepage selection carried through; EUR 300 filter excluded USD offers; two-product comparison and copy/reload restored selections; article-anchor navigation retained the shortlist; 390 px page had no document-wide overflow; table scrolls internally; no captured console errors.
- Full audit remains non-green: 326 errors recorded in `.preview/finder-audit.log`, primarily existing stale evidence and snapshot/content issues. Full release crawl retains 56 existing non-Russian errors in `.preview/finder-crawl-final.log`; Russian finder-related fragment failure was resolved with validated state-fragment handling, not an unrestricted anchor-check bypass.

## Remaining work toward the full objective

### September 8 follow-up: source expiry

- Added hourly revalidation at the Russian layout, so all 26 existing pages can re-evaluate source age without a deployment. ISR is request-driven and the triggering request may receive the previous cached version; it is not an exact-time removal guarantee.
- A simulated-clock rendering test exposed crashes in both FundedNext head-to-head pages after product expiry. Missing comparisons now have a safe fallback, while existing explanatory content and URLs remain intact.
- Four main reviews and both comparisons display a dated recapture warning when relevant product sources expire. Static explanatory passages remain dated editorial material, not freshly verified terms. Other Russian guides still need the same explicit archive labelling for their independent evidence files.
- Added a read-only daily GitHub Actions freshness report, with a 14-day warning queue and a failing status for already expired datasets. It never changes capture dates or publishes a machine-written rule change.
- Platform findings and remaining verification gaps are recorded in `docs/platform-verification-2026-09-08.md`. Removed the unreliable aggregate platform list from the FundedNext/Bright Funded comparison.
- `npm run test:russian-expiry -- --built` passed: all 26 pages at five data-derived dates, plus inherited hourly regeneration in the actual build manifest. Production build passed (509 pages), lint passed without warnings, finder regression tests passed, and local release checks passed all 26 URLs/sitemap/canonicals. Full audit remains 326 errors with output unchanged from the preceding release. No challenge capture or editorial dates were refreshed by this technical update.

1. Verify product-and-tier-specific platform availability before adding a platform filter. Do not reuse aggregate firm platform lists; FundedNext Instant and U.S. configurations differ, and some account sizes have platform restrictions. Keep platform limitations visible meanwhile.
2. Finish source-age presentation for independent guide evidence and resolve the recapture queue. Hourly Russian regeneration and daily reporting are implemented, but regeneration does not recapture facts. Eight firm datasets were already outside the 30-day policy at the initial audit.
3. Finish native Russian editorial adaptation across existing pages; remove internal production jargon and reconcile existing aggregate claims with current official evidence. Edris is the established author; do not invent testing experience or payout verification.
4. Add contextual finder handoffs from the relevant reviews and guides, retaining original explanatory content and existing URLs.
5. Verify Russian funnel reporting against actual affiliate purchases. Browser events and redirects are not proof of paid conversions, and must not be double-counted.
6. Revisit first-party Search Console evidence for the full Russian section and prioritise improvements by query/page results. Do not infer Russian nationality from country traffic or claim ranking gains from a successful build.
7. Develop a small, evidence-led outreach proposition using original comparison examples and rule-change research; no mass guest posts, paid ranking links, unsolicited outreach or unverified backlinks.

### September 8 follow-up: reader-facing reviews and discovery

- Reviewed the full Russian section in authenticated Search Console, not just an individual ranking URL. Raw account data stays in git-ignored `.preview/russian-search-console-2026-09-08.json`; do not put private analytics into public content. The observed reporting period predates the September 8 releases, so it cannot establish their ranking impact.
- Verified the canonical sitemap is HTTP 200, contains all 26 Russian URLs and is already referenced in robots.txt. Submitted that direct URL in Search Console and verified Success. Retained the existing successful `www` submission; its redirect was not established as an indexing blocker.
- Priorities from the inspection: improve the existing reviews, investigate discovery/indexing for the principal partner review and instant-funding guide, and preserve the distinction between rankings, product comparisons and country eligibility. Do not create separate pages for near-identical query variants.
- Removed internal editing/implementation vocabulary from the four main Russian reviews. Bright Funded now credits the established author, Edris Derakhshi, in its byline, profile link and Article schema. Removed unsupported biography claims from the review boxes; the separate author profile still needs an evidence-based review.
- Added source-age-aware two-step comparison handoffs from FundedNext, Bright Funded, FundingPips and FTMO reviews. They start at the same account size, preserve USD/EUR and do not declare a winner. Expired or missing pairs fall back to the generic finder instead of leaving invalid share links.
- Updated four editorial modification dates for these substantive changes without refreshing challenge captures. Finder and expiry tests now check the review handoffs, visible author/schema agreement, prohibited internal vocabulary and expired-state behaviour. The audit's outdated Tara-specific Russian byline guard now expects Edris.
- Remaining: finish native adaptation of other Russian guides and comparisons; confirm actual indexing after discovery requests; verify platform matrices and paid-conversion reporting. A crawlable page, accepted sitemap or no manual action does not prove rankings or comprehensive quality.

Accounts, loyalty points, public review moderation and live payout tracking are deliberately later-stage products, not requirements of the first comparison release.

### September 8 follow-up: comparison usability

- The live competitor inspection confirmed that selection tools, current commercial offers and maintained evidence are the useful model; do not reproduce competitor ratings, codes, AI verdicts or earnings assumptions.
- Consolidated repeated ranking-page content: one disclosed partner section with the two primary partners and secondary FundingPips, plus one clearly scoped broader editorial directory. Kept original country/rule explanations and all existing review URLs. The former `top-5` and `partner-matrix` anchors still reach the corresponding consolidated sections.
- Preserved directory coverage and moved the removed cards' entry-price and drawdown information into its rows. The directory now labels English-only reviews, explains its wider coverage, and uses the oldest source date rather than overstating cross-product freshness.
- Added named selections and individual removal near the finder filters. Both comparison shortcuts move keyboard focus to the table and respect reduced-motion preferences. Existing floating shortcut and shareable selections remain.
- Corrected instant-programme payout and cost wording so it no longer refers to an evaluation stage. The stored days are still request conditions, not promised receipt dates. No prices, source capture dates, affiliate agreements or country permissions were changed.
- Regression coverage now checks phase-0/positive-stage payout labels, actual instant-only rendering, one partner section and directory, all fresh directory rows and retained fragment anchors across source expiry dates. Existing source/layout guards were updated to the consolidated structure, not removed wholesale.

### September 8 follow-up: author integrity and light-layout proposals

- Corrected Edris’s TFH role to Author in the shared registry, metadata and Person schema. Removed unsupported claims about founding TFH, personally funding with four named firms and verifying every payout. TradingRage’s own About page identifies him as its founder; a CryptoPotato article credits his analysis. These limited claims now have visible source links on the profile. No qualifications or personal trading results were inferred from those publications.
- Aligned the existing authors directory and the two remaining Russian founder-claim boxes. Added a biography-specific sitemap update date without changing price captures. Actual author-page rendering/schema checks, 26 Russian pages across five expiry dates, finder tests and targeted lint passed. A rebuilt production preview and deployed release checks for these latest additions are still required.
- The user requested two whole-site light directions because the dark pages are hard to read. Private proposals in `.preview/design/` show Light Teal Platform and Warm Editorial, each with homepage, comparison and review views. They are design previews, not new public SEO pages or a deployed theme. A direction still needs to be selected before the site-wide visual change.
- The light proposals use local-only navigation and shortlist interactions, plus optional host controls for article type size. No real prices, purchase actions, country decisions or affiliate attribution are simulated. Reviewed their desktop/home/article layouts and narrow-screen reflow; the production theme is unchanged.

## Whole-site light platform — September 8 implementation

- User delegated the visual choice and authorized adapting the whole website. Selected Light Teal Platform: off-white page, white surfaces, dark body text, restrained teal actions, and accessible semantic warning colours. Both locales inherit the same root design system; no URLs, source captures, prices, or affiliate destinations were replaced by prototype content.
- Migrated shared CSS and inline UI styles, including rule-colour helpers, navigation, footer, article templates, comparison tools, author profiles and forms. Kept white text only where it belongs on solid teal controls/brand marks; white-logo assets get small brand wells. Imported article HTML has scoped colour compatibility rules without rewriting review prose.
- Both homepages now have usable comparison entry panels. English form inputs feed the existing `size`/`program` query parser; Russian selection keeps the existing shareable finder hash and analytics. Russian home retains the two explicitly disclosed FundedNext/Bright Funded partners and selected guide links. English homepage shows three latest articles instead of six plus a category rail.
- Reviews receive 17–18 px text, comfortable line spacing, bounded reading widths and simplified mobile surfaces. Removed decorative glows and rotating/tilting page effects. Corrected the Russian home navigation item being marked active on every Russian URL and the loading grid overflowing narrow phones.
- Added `npm run check:light-release`: semantic text/control contrast checks and HTTP/compiled-style coverage for every sitemap page plus the 404 template. This is not a claim of full accessibility compliance; actual browser checks cover responsive layouts, visible text, menus, forms and shortlist interaction separately.
- Final production build passed (509 generated routes); all 281 sitemap URLs respond and inherit the compiled light design. Finder release checks passed all 26 Russian URLs; source-expiry rendering and finder regression tests passed. Full lint passed, followed by targeted lint after the final status-colour fix. Full audit findings remain exactly unchanged at 326 errors; release crawl remains the same 56 existing non-Russian errors (both compared against the preceding logs with their UTF-16 encoding handled correctly).
- Browser checks covered English/Russian homepages, FundedNext and Bright Funded reviews, PropLive, the Russian head-to-head, the directory, blog, author/contact templates, English/global and India comparisons, change feed and an imported-HTML guide. Tested desktop and 390/320 px widths; tested Russian menu/home-finder/selection/focused comparison and English `size`/`program` URL handoff. No document overflow on the checked loaded pages; final global comparison and legacy article checks found no low-contrast solid-background text or console errors. Gradient artwork is excluded from the numeric browser contrast probe and inspected visually. Ready for the authorized main/release deployment; production verification follows the push.

### September 8 follow-up: finished Stellar Instant guide

- The whole-site light release above was deployed as `16ee365` to both main and release. Live checks passed all 281 sitemap pages and all 26 Russian routes.
- Finished native Russian adaptation of the existing Stellar Instant guide, retaining its URL, 13 section anchors, official source links and affiliate attribution. Added the shared readable article layout, visible Edris byline and a substantive September 8 editorial modification date. Removed internal implementation vocabulary; clarified the difference between payout eligibility and receiving money, the post-withdrawal loss buffer, and unresolved reset/scaling claims.
- Re-read the official news-rule article on September 8 and restored the previously omitted maximum of three 1% news-related MLL adjustments per account. This is a verified omission in our guide, not evidence that the firm changed its terms on September 8. Only the news evidence subsection has a new capture date; the August 29 rules capture and August 27 price/country captures remain unchanged. No challenge data, price or country eligibility was refreshed by this editorial change.
- The guide shows separate rule, news, price and country-source dates. A reusable evidence notice labels explanations as dated material when any relevant source leaves the 30-day window; FAQ structured data is then withheld. The existing price table still expires independently. Other independent guides remain in the source-age adaptation queue.
- Added a same-size, phase-0 finder handoff for FundedNext Stellar Instant and FundingPips Zero at $10,000. Missing/expired pairs fall back to the generic finder. Bright Funded stays a disclosed evaluation-based alternative, not an invented instant programme. The Free Trial offer now asks readers to verify programme eligibility instead of implying that every Instant purchase qualifies.
- Verification: 509-route production build; 26 Russian pages rendered at eight source-derived dates; guide notice tests for mixed, missing and future dates; finder regression tests; all 26 Russian release/sitemap checks; all 281 light-theme URL checks. Full audit remains exactly the preceding 326 findings; the full crawl remains the same 56 non-Russian errors. Browser checks passed desktop and 390 px article reading, internally scrolling tables, the two-selection handoff, and focused comparison; no document overflow or captured console errors. Temporary viewport override was reset.
- Follow-up from actual comparison QA: the finder currently labels a null daily-loss value as unknown even where the programme guide documents no separate daily limit. Resolve this through explicit source-backed data semantics, not a global null-to-zero substitution. Continue independent-guide adaptation and the stale-evidence queue; do not add near-duplicate articles or claim indexing/ranking gains from this release.

### September 8 follow-up: comparison evidence and discovery

- Removed the warm-process cache of freshness-filtered products. The comparison hub and all 171 existing matchup routes now inherit hourly regeneration; counts and editorial overlays re-evaluate their source-age gates. Request-driven ISR can briefly serve the preceding cached response and does not recapture source facts.
- The current directory has 55 two-sided matchups across 11 firms and 53 products on September 8. The other 116 existing pages remain searchable under a separate, normally collapsed "Awaiting source checks" section. All 171 canonical URLs and sitemap entries remain; this is evidence coverage, not a claim that excluded firms closed.
- Partial comparisons keep the available firm's product rules and sources. Excluded products receive their original official source links and recorded dates for rechecking. When either side lacks current evidence, the page withholds two-sided cost tables, editorial winners and editorial FAQ schema instead of restoring aggregate fee/risk tables. The four retained firm-context rows explicitly are not independently refreshed programme terms.
- Grouped source citations use the oldest supporting product date; the evidence summary distinguishes the range of source checks. Sitemap modification dates reflect this genuine template edit and each pair's own captures without changing any challenge capture, price, country permission or affiliate destination.
- Added warm-process expiry tests for all 171 pair models at 11 source-derived dates plus a clock reset. Actual hub and full/partial/unavailable detail rendering, metadata, preserved pending-page discovery, overlay expiry and the built regeneration intervals are covered. Russian expiry and finder tests still pass.
- Browser testing found and fixed the search field's accessible name changing when its clear button appeared. Current and pending results share the search but retain distinct status labels. Phone testing also caught missing shared section gutters from the light rollout; restored the 1.5rem side spacing and added a light-release guard.
- Verification before release: production build (509 routes), full lint, 26 Russian release checks and 281 light-theme sitemap URL checks. Full audit remains the same 326 findings (334 reported bullet lines, compared without encoding loss). Full crawl falls from 56 to 28 errors, all on existing English country/style landing-page count fixtures; no comparison or Russian crawl failures remain. Those remaining checks were not deleted or relaxed to obtain a clean result.
- Still required: stale firm-source recaptures, independent Russian guide adaptation, country/style landing-page evidence counts, product-specific platform verification and conversion measurement. Comparison social images also retain their earlier design/aggregate fields and need a separate evidence-aware update. No indexing, ranking or paid-conversion gains are established by this release.
- Release status: `58c9642` was pushed to main and release before the user changed the release policy. GitHub reported production deployment `6325429694` completed successfully when cancellation was checked. The user now requires local-first work to conserve paid Vercel build minutes: batch and preview locally; no further remote builds or pushes to Vercel-connected branches without fresh explicit approval. This instruction is also recorded in `AGENTS.md` and these notes remain local.

### Local work in progress: MT5 guide and Godly design reference

- Re-read FundedNext's official platform, EA, Free Trial, PC login and strategy-continuity pages on September 8. The general EA article now states a manual-only cutoff at $50,000 inclusive; the prior guide capture omitted this. Only its EA subsection date was refreshed. The product-specific Instant EA article does not repeat the cutoff or establish an exemption; recorded this as a separate dated scope watch, not a promised exception. Challenge prices and captures are unchanged, and existing product EA values remain `restricted`.
- Draft local edits adapt the MT5 guide into readable Russian, remove frozen programme/price counts from metadata and navigation, add the Edris byline and source-age notices, preserve existing anchors/affiliate attribution and add a dated EA condition across related Russian pages. Finder cards link to the rule explanation. These edits are NOT deployed, and their new targeted tests, full lint/audit, rebuilt preview and browser QA are still required. The pre-existing Russian expiry suite passed before the final shared-notice insertion, but did not yet cover the new MT5-specific contracts.
- User supplied https://godly.design/ during this work. Inspected its Websites gallery and the Agentwork and Gumloop reference pages. Applied the layout principles to a first local Russian homepage slice, without copied assets, text or ratings: stronger typography and whitespace, a working comparison preview near the top, quieter disclosed partner cards, concise decision guidance and three selected article links. The preview uses the existing fresh product projection and filters; it shows one programme per firm alphabetically, preserves USD/EUR prices and hands selections into the full finder via fragments. It is not a paid ranking or a claim that the first three examples are the best offers.
- The local HMR preview is http://127.0.0.1:3214/ru and returned HTTP 200 after the slice compiled. No browser visual sign-off is claimed for this slice. Added automated homepage result/order/handoff checks and MT5 article/source/schema expiry checks; all 26 Russian pages pass at eight source-derived dates. Finder regression tests and targeted lint pass. The production build passed compilation and TypeScript but failed during prerendering at `/prop-firm-challenge-changes`: its social-card guard expects 16 entries/9 firms/5 verified/11 watches, while the new ledger has 18/9/6/12. The guard and static image have not been bypassed or falsely marked updated. Final copy/label-size refinements also require inclusion in a subsequent successful build before any release.
- Full local audit reports 331 errors versus 326 before this batch: two new rule-watch records currently conflict with the audit's requirement that every watch recheck match every affected product capture, and three fixed change-feed social counts are out of date. Resolve independent rule-evidence provenance explicitly; do not refresh price captures without checking prices or suppress these findings. The existing 326 findings remain outstanding. Release crawl fixtures were adapted to native MT5 wording, but the full crawl has not yet been rerun for this slice.
- Next local batch: resolve the evidence-watch integration, finish verification, and carry the chosen hierarchy into the remaining shared templates. Keep all work local until a fresh explicit deployment approval.

### Local follow-up: independent rule-watch provenance

- Added explicit `ruleEvidenceRefs` for the two September 8 FundedNext watch records. Each reference names a flat JSON evidence file and a section; the audit checks firm identity, exact source coverage and matching rule-check dates. Price and lineup watches cannot use this exception. Legacy full-product capture-date checks remain unchanged. Added the originating firm to the MT5 evidence document without changing its older root/platform/price dates.
- The public feed identifies rule-only checks as separate from price refreshes, and its freshness FAQ now makes that distinction. Added `npm run test:watch-evidence` for real entries, unchanged price captures, legacy behavior, missing/malformed references, cross-firm citations, duplicate references, dates and incomplete source coverage. The 26-page/eight-date Russian rendering suite also verifies both rule-only feed notices and home-preview fee denominations, subscription/first-payment labels and empty state.
- TypeScript, targeted lint and the expanded rendering/provenance tests pass. The full audit is 329 errors: comparison with the preceding release log confirms the only three added findings are the unchanged challenge-change social-card count guards. No older findings were removed or relaxed.
- The site-building skill requires explicit approval to refresh the existing social-sharing image; approval was requested asynchronously and has not yet been received. The image, its metadata and its build consistency guard are preserved. Do not claim a successful production build until this mismatch is legitimately resolved and the build is rerun.
- Local Russian HTTP/sitemap verification remains incomplete. Cold development compilation exceeded the normal 20-second release request budget, so the verifier now has an explicit localhost-only `--dev` mode allowing 60 seconds per request; ordinary release timing remains unchanged. The local run then timed out while compiling `/ru/obzor-bright-funded`; the retained dev server subsequently logged HTTP 200 after 65 seconds. The checker terminated with failure, not a successful all-URL result. Do not restart the server on this evidence; resume URL verification against the same warmed server or a successful production build. This is not a production performance result. No deployment or push was performed.

### Local follow-up: English homepage comparison parity

- Extended the selected light, comparison-first direction to the English homepage hero. Its existing GET comparison form now displays up to three real programme examples, one per firm alphabetically, with account-size/phase filtering, source dates, market labels, explicit currencies and base/monthly/initial-payment labels. Each example carries its valid product key into the existing full comparison. The form remains usable without JavaScript, partner status does not order examples, and empty source coverage retains a directory link. Lower homepage sections, existing affiliate routes and social metadata are preserved.
- Added hourly regeneration to the English homepage: its freshness-dependent content must not remain a deployment-time snapshot. The root sitemap lastmod now follows this September 8 editorial change and its own supporting firm/challenge/watch data, without bumping unrelated URLs or changing evidence capture dates.
- The English local preview returned HTTP 200 at http://127.0.0.1:3214/. The expanded rendering test passes all 26 Russian pages plus the actual English homepage at eight source-derived dates; checks include both locales' fee currencies, billing labels, empty states, current result ordering and valid comparison handoffs. Added an English-root regeneration check to the built-manifest mode, which still requires a successful new production build. No new browser visual sign-off is claimed.
- Follow-up verification: TypeScript and targeted lint pass. The full audit remains exactly 329 errors, with no finding-line differences from `.preview/local-watch-evidence-audit.log`; the homepage slice did not introduce additional audit findings. Logs: `.preview/local-home-parity-tests.log` and `.preview/local-home-parity-audit.log`.
- Resuming the Russian URL verifier progressed through the warmed routes and successfully rendered the MT5 guide, but timed out at `/ru/fundednext-stellar-instant`; the full URL result remains incomplete. Read-only host memory reporting showed approximately 0.6 GiB free of 7.7 GiB during the slow compilation. This may contribute to local timings, but is not a verified production-performance diagnosis. Keep verification sequential and retain the existing dev server; do not change or stop unrelated applications.
- The social-image approval request is still pending. Preserve its image and build guard, keep all edits local, and do not claim the overall objective or release is complete.

### Local follow-up: cTrader guide adaptation and evidence

- Reworked the existing `/ru/prop-firmy-s-ctrader` guide into reader-facing Russian, retaining its title/H1, URL, established FAQ anchor, data markers and all six affiliate-attribution routes. Added the shared readable article layout, visible Edris byline, September 8 editorial date, platform/price source-age notice and all nine official platform-source links. No new SEO article or speculative platform filter was added.
- Re-read the nine primary pages, including the product-specific Stellar Instant platform list. Refreshed only `russian-ctrader-evidence.json`; no challenge price/capture changed. Stored the existing size and fee evidence quotes, the U.S. refund-rule qualification and Instant's published platform scope. This records verified distinctions, not a claim that these policies changed on September 8. Bright Funded's missing cTrader fee, size cap and cBot compatibility remain unknown.
- FundedNext platform-cost examples now require both fresh product prices and fresh platform-fee evidence. Missing/expired combinations render an explanation instead of an empty table or an implied all-in price. Bright Funded base-price references keep their own dates and preserve euro cents; they are not represented as confirmed cTrader checkout prices. FAQ structured data is withheld when a supporting rule or price check expires. The general finder handoff explicitly does not verify cTrader availability for a user's profile.
- Expanded rendering tests pass all 26 Russian pages plus the English homepage at eight source-derived dates. cTrader-specific checks cover independent platform-price expiry, malformed/future platform dates, fee arithmetic, named authorship, native copy, source links, the U.S. exception and a properly qualified finder handoff. TypeScript and targeted lint pass. The full audit remains exactly 329 findings, compared with the prior log; no checks were relaxed to hide unresolved source or social-card findings.
- Final local verification passes all 26 Russian routes, sitemap/canonicals/H1/language, comparison rendering and the new cTrader-specific assertions. During the preceding attempt Next.js automatically restarted at its memory threshold, reset an HTTP connection and then incorrectly returned 404 for the existing ranking route. Confirmed the source file still existed with no not-found branch, stopped only the retained preview session and restarted it at the same address; the ranking returned HTTP 200 and the full verifier then passed. No files, caches or unrelated applications were removed. This is local verification, not a successful production build or deployment. Logs: `.preview/local-ctrader-expiry.log`, `.preview/local-ctrader-audit.log`, `.preview/local-ctrader-russian-urls-recovered.log`.

### Local follow-up: focused English homepage

- Simplified the English homepage to four sections: the working programme comparison; one clearly disclosed FundedNext/Bright Funded partner section; compact decision-tool links; and three selected existing guides. Removed duplicate headline-stat tiles, spotlight, partner rail, top-five list, preset picker, inline cost demo, logo marquee and final sales CTA from this page. Existing directories, comparison tools, articles and the complete cost guide remain reachable; no content routes were deleted. The newsletter stays configuration-gated and moves below the guides.
- Applied the selected light layout with restrained typography, open partner columns, readable links and single-column phone layouts. Removed this page's decorative animation components and aggregate score/discount/platform claims. Partner cards retain their existing campaign attribution, official `/go/` routing and sponsored relationship labels. Their programme counts and oldest supporting dates require fresh evidence; stale cards keep review links with an explicit warning. Commercial placement never changes the alphabetical finder results. No challenge values, capture dates, social images or metadata were changed by this homepage edit.
- Expanded the actual-page rendering suite to assert section order, exactly two primary partners, three guides, preserved tool discovery, affiliate attributes and partner-source expiry. All 26 Russian pages plus the English homepage pass at eight source-derived dates. Finder regression tests, TypeScript, targeted lint and whitespace checks pass. The full audit remains exactly 329 findings, with no finding-line difference from the preceding cTrader log. The cost-model audit now verifies homepage discovery of the complete guide and rejects a returning inline calculator; the reusable calculator's existing math/label/verdict checks are retained.
- Added `npm run check:home-release -- <base-url> --dev` for read-only local HTTP/rendered-content checks. It follows actual homepage review links, form destinations and guide/tool links, without requesting affiliate destinations. It accounts for App Router content streamed after the main landmark and does not mistake serialized RSC text for rendered headings. The homepage and every checked destination except `/prop-firm-challenge-changes` render successfully. That existing page streams an error after HTTP 200, so the checker correctly remains non-green rather than reporting all links healthy.
- The fresh local production build passed compilation and TypeScript, then failed at the unchanged challenge-change social-card guard: expected 16/9/5/11, ledger 18/9/6/12. This confirms the outstanding image-approval blocker; the image and guard remain intact. No production-build success, browser visual sign-off, deployment or ranking improvement is claimed. The continuous local preview remains http://127.0.0.1:3214/. Logs: `.preview/local-home-focused-expiry.log`, `.preview/local-home-focused-audit.log`, `.preview/local-home-focused-rendered-links.log`, `.preview/local-home-focused-build.log`.
- Next work remains the Russian guide/evidence queue (including forex), verified product-specific platform coverage, outstanding audit findings and whole-site readability. The overall objective is not complete; keep all work local until fresh deployment approval.

### Local follow-up: Russian forex guide and leverage correction

- Adapted the existing `/ru/forex-prop-firmy` article into readable Russian while retaining its URL, original product-table/FAQ anchors, all four affiliate campaigns, disclosure markers and local-versus-global product boundary. Added the shared light article layout, Edris byline, source list, separate evidence dates, source-age warnings and a same-size two-step comparison handoff. Removed fixed seven-product metadata/headings, mixed internal English jargon, uncoupled payout/refund-price examples and unqualified aggregate platform claims. No new article was created.
- Re-read seven first-party forex pages and both image tables embedded in FundedNext's leverage article. Corrected Stellar 1-Step from 1:100 to 1:30 for both stages; Stellar 2-Step/Lite remain 1:100 and Instant remains 1:30. Recorded the correction visibly and in a scoped verified watch with independently dated source evidence. This corrects our August capture; it is not a claim that FundedNext changed policy on September 8. Only forex evidence was refreshed, not challenge prices, payout terms or country access. Bright Funded's linked instrument-sheet count remains explicitly unverified.
- Price rows expire on their own product dates; leverage and current instrument-count summaries expire on separate firm-source dates. Unknown targets are no longer labelled as zero stages. Empty tier data cannot crash rendering, missing prices/limits remain unknown, both currencies preserve cents, and tier-specific dollar loss caps take precedence over percentage-derived examples. The table explains that these are initial limits, not a running account's remaining loss allowance. The margin example is explicitly hypothetical and does not calculate a True-Cost table or promise trading outcomes.
- Expanded actual-page tests cover separate source expiry, invalid/future evidence dates, corrected leverage, unknown targets, missing second-stage targets, empty tiers, unknown fees/limits, currency precision, cash-cap overrides, authorship, source links and comparison handoffs. All 26 Russian pages plus the English homepage pass at eight source-derived dates. Provenance tests now validate all three rule-only records. TypeScript, targeted lint and whitespace checks pass. Full audit remains 329 findings; the only changed finding lines are the social-card expected totals, now 19 entries and 7 verified notes. No challenge capture file changed.
- All 26 Russian HTTP/rendering/sitemap checks pass, including the forex correction and its genuine editorial lastmod. The first attempt encountered a connection reset when Next automatically restarted at its memory threshold; rechecked the same live server, confirmed the ranking returned 200 with its finder, and reran successfully without manually restarting it. Logs: `.preview/local-forex-expiry.log`, `.preview/local-forex-audit.log`, `.preview/local-forex-russian-urls-rechecked.log`.
- The fresh local production build passed compilation and TypeScript, then terminated at the existing `/prop-firm-challenge-changes` sharing-image guard: expected 16/9/5/11, received 19/9/7/12. Confirmed the build process exited with code 1; `.preview/local-forex-build.log` records the terminal failure. The image and guard remain unchanged pending approval. No full-build success, browser visual sign-off, deployment or ranking improvement is claimed. The local preview remains http://127.0.0.1:3214/ru/forex-prop-firmy.

### Local follow-up: TeamTraders review adaptation

- Applied the existing light article shell to `/ru/obzor-teamtraders`, with a contents navigation, Edris byline, fixed original publication date and September 8 editorial date shared with the sitemap. Replaced mixed English terminology and unsupported comparative praise with native Russian explanations. The review explicitly distinguishes published firm terms from independently verified customer reviews or proof of payouts. No new content route was created.
- Rechecked the public offer and older documentation, and inspected the current FAQ. Registration is described as acceptance in offer section 1.3, while compensation is placed in the account area by section 6.1. Corrected every decision-path recommendation to read the offer and request inaccessible terms before registering, even for a free demo. Preserved the three documented discrepancies, local-versus-global market boundary and direct non-affiliate TeamTraders source links. This editorial/source check did not constitute a complete new product capture; the existing August 28 evidence dates and all challenge prices remain unchanged.
- Local evidence dates now independently gate headline statistics and FAQ schema. After expiry, the substantial dated analysis remains with explicit recapture warnings instead of becoming a thin empty page. Added a complete four-source list. Global partner counts expire on their own price records; they do not inherit the TeamTraders date or imply verified country/payment access. FundedNext and Bright Funded retain their disclosed affiliate campaigns. A separate same-size two-step CFD comparison link falls back to the dated finder when its preset expires, without promising two available programmes.
- Expanded actual-page rendering tests cover each source's missing/malformed/future/old date, archive retention, independently fresh partner records, eight source-derived time boundaries, contents targets, source links, confirmed authorship, publication/editorial dates, legal-first decision order, native wording, affiliate attribution and comparison fallback. All 26 Russian pages plus the English homepage pass at eight dates. TypeScript, targeted lint and whitespace checks pass. Full audit remains 329 findings with no finding-line difference from the preceding forex audit. Logs: `.preview/local-teamtraders-expiry.log`, `.preview/local-teamtraders-audit.log`.
- All 26 Russian HTTP/rendering/sitemap checks pass on the same continuous local server, including the TeamTraders contents, global comparison handoff and genuine editorial lastmod. The first HTTP verifier reached TeamTraders but failed a raw-string date assertion because React inserts HTML comment separators. Updated that assertion to compare normalized visible text without weakening the required editorial date; the rerun passed (`.preview/local-teamtraders-urls-rechecked.log`).
- The fresh production build passed compilation and TypeScript, then exited with code 1 at the unchanged challenge-changes social-card guard (expected 16/9/5/11; received 19/9/7/12). Terminal result recorded in `.preview/local-teamtraders-build.log`. No full-build success, browser visual sign-off, deployment or ranking improvement is claimed. All work remains local; the sharing image and guard are unchanged pending explicit approval. Preview: http://127.0.0.1:3214/ru/obzor-teamtraders.

### Local follow-up: stale CFT evidence investigation and capture holds

- Investigated Crypto Fund Trader, one of the eight expired firm datasets. First-party homepage, FAQ, terms, refund and Break-page checks found material errors/uncertainty beyond the July 27 dates: Instant's scaling split versus the generic 80% claim, Ascend's fixed completion award rather than an ongoing funded split, missing Break activation fees, conflicting base fees/minimum days, uncertain Break availability and conditional traded-day/calendar-day payout timing. Findings and exact first-party links are recorded in `docs/cft-source-refresh-2026-09-08.md`.
- Prepared a held five-product/24-tier candidate in `.preview/cft-capture-2026-09-08/capture-crypto-fund-trader.json`. The actual merge preview reports 21 material differences (6 payout, 3 pricing, 9 risk, 3 rules); its ten release blockers remain unresolved. This is a draft observation set, not an approved capture or complete current catalogue. No challenge record, review, capture date, public watch or page was changed in this investigation. Those public corrections still require implementation after the source/model questions are resolved; the old review is not repaired by these notes.
- Added optional `reviewStatus` and `releaseBlockers` validation to the existing merge workflow. Drafts and captures with blockers remain previewable but cannot be written, even with `--accept-changes`. Invalid/skipped merges now return exit code 1 instead of being mistaken for successful updates. Legacy captures retain the existing explicit material-diff acknowledgement requirement; price protection and provenance archiving remain intact.
- Added `npm run test:capture-merge`, which executes the real CLI in an isolated scratch repository and verifies target preservation, no archive on held writes, malformed inputs, rejected material changes, price-loss protection and successful legacy/ready captures. Tests and targeted lint pass. Full audit remains exactly 329 findings, with no finding-line difference from the preceding TeamTraders log (`.preview/local-capture-hold-audit.log`). No new application build was run for this script/research-only change; the last production build remains blocked by the unchanged sharing-image guard. Nothing was deployed.
- Next data work: resolve CFT purchase/source conflicts and correctly represent its fixed scholarship and stage-specific reward/risk conditions; update the existing review and affected Russian crypto explanations, regenerate applicable cost tables and publish a scoped watch only after editorial review. Continue the remaining stale-firm and Russian-guide queue. The overall goal is not complete.

### Local follow-up: comparison readability beyond the homepage

- Continued the selected light platform design on the actual comparison surfaces, without adding routes or copying reference-site artwork. Global comparison controls, firm names and prices now use 1rem text; its inline data labels no longer use the previous 0.64–0.82rem sizing. Added explicit keyboard-focusable, labelled horizontal scroll regions to its main table and shortlist decision matrix. Filtering, shortlist state, data, affiliate destinations and ranking calculations are unchanged.
- Extended shared readable typography to Russian finder filters, card facts, payout explanations, source dates, shortlist controls and the selected-programme table. Added wrapping for card actions, long names/facts and the floating shortlist; filters become a single column on narrow screens without shrinking their text. Shared English shortlist and decision-matrix styles now use the light surfaces, larger text and 44px primary selection/removal controls. These are implemented CSS/markup changes, not a new browser visual or interaction sign-off.
- Expanded the real-page rendering tests to include the global comparison at every tested source-age date, asserting its accessible scroll-region markup, data-table styling hook and expired-source empty state. Added a regression guard against sub-14px inline comparison text. The existing 26 Russian pages and English homepage still pass at eight source-derived dates; finder regression tests, targeted lint and whitespace checks pass. Local compilation and TypeScript pass; final build/audit results are recorded below when terminal.
- The overall release remains incomplete. Keep the unanswered sharing-image approval boundary intact and preserve the CFT correction/stale-source queue. A further data-rendering issue noticed during this pass needs a separate verified correction: the global comparison still translates some null minimum/maximum-day and consistency values into “None”, “Unlimited” or “none published”, despite its unknown-data explanation. Do not conceal that semantic issue with the typography changes.
- Terminal verification: the final rendering-test rerun passes, and both `/prop-firm-challenges` and `/ru/luchshie-prop-firmy` return HTTP 200 without the checked Next error marker on the retained localhost server. Full audit exits 1 with exactly the same 329 findings as the preceding capture-hold log. The local build passes compilation (25.9s) and TypeScript (27.2s), then exits 1 at the unchanged challenge-change sharing-image guard (expected 16/9/5/11; received 19/9/7/12). No successful production build, browser layout/interaction sign-off or deployment is claimed. Logs: `.preview/local-comparison-readability-tests.log`, `.preview/local-comparison-readability-finder.log`, `.preview/local-comparison-readability-lint.log`, `.preview/local-comparison-readability-audit.log`, `.preview/local-comparison-readability-build.log`.

### Local follow-up: distinguish missing evidence from absent rules

- Corrected the underlying trading-day/consistency null semantics rather than only changing one table's wording. A verified minimum of zero stays zero; a null minimum is unverified. Added optional `maxTradingDaysUnlimited` and `consistencyRuleApplies` fields for explicit first-party confirmation of unlimited time or an absent consistency rule. Existing null records are not automatically converted into those confirmations. No challenge prices, raw captures or verification dates were changed; source recapture/migration remains necessary where legacy nulls conflated the states.
- Added client-safe shared English/Russian labels and propagated the optional fields through the global/Russian projection and India matcher. Global and India tables, English head-to-head rule groups, Russian finder details and the Russian selected-programme table now distinguish numeric, unknown, explicit-absence and contradictory data. Corrected misleading null fallbacks in the Russian FTMO review and instant-funding guide. Only the instant guide needed a new route editorial date (September 8); the other affected Russian routes already carry that date. No URLs or social metadata changed.
- Capture merging now rejects malformed flags, contradictory finite/unlimited or percentage/absent-rule combinations, invalid day counts and invalid consistency percentages. Flag changes participate in the material diff and require explicit acknowledgement; accepted captures retain the flags. Documented these semantics in AGENTS.md. Verified with real CLI fixture runs and shared-label/projection tests; no actual firm capture was merged.
- Real rendering fixtures cover the English, Russian and India comparison components with missing values, zero, finite values, explicit absence and contradictions. All 26 Russian pages and the English homepage continue to pass at eight source-derived dates. Head-to-head tests pass 171 pair models over 11 dates plus clock reset, asserting legacy nulls never become permission. Targeted lint and whitespace checks pass. Full audit remains the same 329 findings, with no changed finding lines (`.preview/local-rule-absence-audit.log`).
- The local build passed compilation (21.4s) and TypeScript (24.5s), then exited 1 at the unchanged sharing-image guard. Preserve the image and guard pending approval; no successful full build or deployment is claimed. The first local HTTP pass verified the global and Russian finder, then lost its connection when Next automatically restarted at its memory threshold. The retained server reported Ready again; remaining URL checks were resumed against that same server without a manual restart.
- Remaining work includes CFT corrections, other stale datasets, explicit evidence capture for the new absence flags, stage-specific rule coverage and the broader delivery queue above. This change does not establish product access, ranking improvement, verified payouts or whole-site visual sign-off.
- Resumed HTTP checks completed successfully for `/ru/obzor-ftmo`, `/ru/prop-firmy-bez-chelendzha` and `/best-prop-firms-in-india/compare`; all five affected URLs checked across the two passes returned HTTP 200 without the checked Next error marker. Logs: `.preview/local-rule-absence-http.log` (interrupted first pass), `.preview/local-rule-absence-http-resumed.log` (successful remaining routes), `.preview/local-rule-absence-rendering.log`, `.preview/local-rule-absence-matchups.log`, `.preview/local-rule-absence-lint.log`, `.preview/local-rule-absence-build.log`. Everything remains local and uncommitted.

### Local follow-up: simplify shared navigation

- Revalidated the current homepages after the Godly reference discussion: their focused finder/partner/selected-guide layouts already implement the chosen direction. The remaining header still had seven English top-level entries and an 18-link firm dropdown. Reorganized it into four task groups (firms, guides/reviews, country, about), retaining every previous destination. The logo and mobile Home link retain homepage access. Compare and the paired EN/RU language switch stay directly available; source-check coverage moves into the firm disclosure instead of occupying another header badge. No content route, affiliate campaign, ranking, firm data or social metadata was changed in this navigation pass.
- Extended the light shared treatment with readable 44px dropdown links, a two-column larger disclosure, viewport-bounded scrolling, and a 1000px mobile breakpoint. Both languages now use a native modal dialog on mobile, with a visible localized close action, native Escape/focus containment, scroll restoration and closure on a desktop resize. Removed decorative mobile overlay elements. Desktop disclosures now use ordinary navigation links rather than incomplete ARIA application-menu roles; Escape restores focus to their trigger and leaving a group dismisses it.
- Added `npm run test:navigation`: actual component rendering and controlled hook/DOM lifecycle fixtures verify all previous English destinations, Russian partner/directory/local-firm access, paired language links, disclosure IDs, close/Escape/resize callbacks and listener/scroll cleanup. It does not prove browser layout, native focus containment or visual quality. Tests and targeted lint pass. The retained local server is still session 24767 at http://127.0.0.1:3214; `/`, `/ru` and `/ru/obzor-fundednext` return 200 with the new navigation markup. No browser UI QA was performed.
- The local production build passed compilation (36.0s) and TypeScript (26.4s), then terminated with exit 1 at the unchanged challenge-change sharing-image guard: expected 16/9/5/11; received 19/9/7/12. Log: `.preview/local-navigation-build.log`. Full audit exits 1 with 294 errors and 3 warnings (`.preview/local-navigation-audit.log`). This is not a clean release and the count difference from the earlier 329 baseline is not attributable to navigation: the worktree also contains the intervening, unfinished CFT product/review correction. That correction still has stale-product and aggregate-payout findings and needs its public watch/downstream follow-through. Preserve those changes and finish their validation separately.
- Overall delivery remains incomplete. Browser visual/interaction sign-off, source/data and remaining editorial audit work, and the sharing-image approval are outstanding. No push or deployment was made; keep the continuous preview local and require fresh explicit approval for a remote release.

### Local follow-up: CFT correction through the public explanations

- Revalidated the intervening mixed-age CFT capture and full existing review, and re-read the firm's public terms, FAQ, homepage and Break page. Removed the aggregate card's unsupported single profit-share, payout-frequency and drawdown classification; preserved the older firm/Trustpilot dates. No other firm record changed. The three checked programmes remain dated September 8; Ascend and Break remain July 27 with no verified entry fees. The complete capture/review correction is documented in `docs/cft-source-refresh-2026-09-08.md`.
- Added separate ledger entries for the verified reward/timing correction and unresolved Ascend/Break price/availability conflict. A new, narrow `sourceConflictEvidenceRefs` path allows a current conflict observation without falsely refreshing an old product: unresolved source-conflict status only, flat evidence-file paths, quoted observations, matching dates, source coverage and at least two cited sources per affected product. It cannot be mixed with rule-only references or promoted to a verified price change. The actual feed labels this independent observation date. The shared data guard and expanded negative tests enforce these distinctions; existing full-capture and rule-only validation remains intact.
- Added substantive CFT explanation to the existing Russian crypto guide, including the 50% initial Instant share, conditional reward-request timing, Ascend's different reward contract and unresolved Break purchase information. Retained the complete English-review URL and used the supported product-focus query for the watch handoff. Corrected the shared crypto watch's false claim that all five products are still unchecked, but retained the July market/platform evidence date and kept CFT outside the crypto ranking. No new article, product, route, platform eligibility or country-access claim was added.
- Tests pass: actual capture CLI safeguards; CFT mixed-age/unknown-price/activation/split invariants and exact matching of all three generated True-Cost tables; rule/conflict provenance with invalid promotion/scope/path/quote fixtures; actual feed rendering of both records and source links; all 26 Russian pages plus the English homepage at eight source-derived dates; targeted lint. The final rendering rerun corrected a duplicate test variable introduced while adding feed assertions; all assertions remain enabled. This is not browser visual or interaction QA.
- Both `/blog/crypto-fund-trader-review` and `/ru/luchshie-kripto-prop-firmy` return 200 with their corrected content on the retained local server. Their actual `/sitemap.xml` entries both carry September 8 lastmod. Full audit exits 1 with 294 errors and 3 warnings: the CFT aggregate mismatch is gone, while the extra firm in the ledger adds another sharing-card count mismatch. The CFT review itself retains its strict July-27 freshness failure; its section/word-count/generated-math checks show no errors. Do not report this review as mergeable or erase that failure by relabelling incomplete captures.
- The local build passed compilation (31.5s) and TypeScript (69s), then exited 1 at the unchanged challenge-change sharing-card guard: expected 16/9/5/11, received 21/10/8/13. The image, copy constants and approval boundary remain intact. Logs: `.preview/local-cft-followthrough-build.log`, `.preview/local-cft-followthrough-rendering.log`, `.preview/local-cft-followthrough-capture-tests.log`, `.preview/local-cft-followthrough-audit.log`, `.preview/local-cft-followthrough-scoped-audit.log`. No push, deployment, completed full build, verified purchase/payout or SEO improvement is claimed.
- Next work remains the unresolved source queue, crypto guide market-date/FAQ/metadata aging and language adaptation, whole-site visual verification, and the other release audit findings. Keep the full objective active; the above is a completed correction pass, not proof that the complete site is ready.

### Local follow-up: Russian crypto guide language and independent evidence expiry

- Finished the pending adaptation of the existing crypto guide rather than adding an article. Replaced mixed internal/English jargon with reader-facing Russian, clarified instrument versus payout-method evidence, removed an unsupported country-specific shortcut, and retained substantive risk, selection, CFT correction and source sections. The programme-cost table explains its USD and first-cycle assumptions; missing drawdown/share information is not permission or a published policy. Existing anchors, review links, primary partner attribution and the separate Bright Funded payout placement remain.
- The working evidence file records September 8 first-party instrument rechecks for FundedNext, FundingPips 1 Step Flex and Maven, plus Bright Funded's payout-method source. These are narrow source checks, not a price recapture or country-eligibility verification. Product mappings remain four/one/seven; Maven Prediction and CFT remain outside this ranking. Product capture dates were not refreshed by the guide work.
- The actual page now evaluates instrument dates and mapped programme dates/prices on every render, with hourly regeneration. Missing, invalid, future or expired market evidence cannot inherit freshness from newer prices. Missing/unpriced/stale mapped products likewise prevent an incomplete firm snapshot. Payout-source expiry affects its own status and FAQ schema, not independently supported trading-firm cards. An empty current selection keeps the dated long-form guide and sources but no empty current-price table or ItemList. Search title/description no longer promise fixed inventory counts; ItemList links use Russian reviews when available.
- Preserved sharing title/description and images under the Sites skill's explicit-approval boundary. The old crypto sharing text still states three firms/twelve products and needs a separately approved update before release; this is a known remaining inconsistency, not a finished metadata handoff. The existing challenge-change sharing image/count guard also remains unchanged.
- Added actual component-rendering negative fixtures for every ranked firm's market source (including impossible calendar dates), independent payout expiry, missing/stale/unpriced mapped products, empty results, localized schema URLs and restoration of all in-memory fixtures. Expanded source-derived clock coverage to ten dates across all 26 Russian pages plus the English homepage. Tests and targeted lint pass. The full audit remains exactly 294 errors and 3 warnings, with no added or removed findings compared with the CFT-follow-through baseline. Logs: `.preview/local-crypto-guide-rendering.log` and `.preview/local-crypto-guide-audit.log`.
- Local build compilation (44s) and TypeScript (22.9s) pass; the build then exits 1 at the unchanged challenge-change sharing-card guard (expected 16/9/5/11; received 21/10/8/13). Log: `.preview/local-crypto-guide-build.log`. No full-build success, browser visual sign-off, ranking increase, push or deployment is claimed. The broader source queue, release audit and whole-site visual verification remain outstanding; the full objective stays active.
- The retained local server returned HTTP 200 for both crypto guides. Russian output contains the new comparison, twelve mapped programmes and the sources section; the actual sitemap retains the Russian route with September 8 lastmod. This verifies served content, not browser layout.

### Local follow-up: article reading and navigation across languages

- Refined the existing Light Teal reading surface, without new articles or decorative assets: rem-based body sizing, bounded paragraph measure, full-width scrollable tables, readable table headings, larger contents links and restrained sidebar styling. The shared Russian review styles receive the same readable table/contents treatment. Added distinct named keyboard-scroll regions to all four FundedNext and three Bright Funded Russian review tables, including their expired-data states.
- Replaced the English article's two independent heading-slug implementations with one preparation pass shared by the rendered body and contents. It preserves authored anchors and the legacy English auto-slug convention, handles repeated headings/reserved IDs/multiline headings, decodes entities for navigation labels and supports non-Latin fallback fragments. This helper processes trusted repository HTML; it is not a sanitizer for untrusted submissions. Tables get labelled focusable overflow wrappers while their contents remain byte-identical. Source files and generated True-Cost calculations were not edited.
- The English article byline now links to the existing author profile when one is known; no biography claim changed. The existing sidebar retains every prior destination and adds the working programme-comparison route. Affiliate disclosure, outbound attribution, firm summaries and article metadata are preserved. Removed two already-hidden decorative hero elements; sharing assets and metadata remain untouched.
- `npm run test:article-content` passes across all 37 published source articles and 161 unchanged tables, including authored/duplicate/reserved/non-Latin anchor fixtures, table preservation and three actual rendered blog pages. The 26-Russian-page/English-home expiry suite passes at ten source-derived dates with additional named-table assertions for both primary Russian partner reviews. Targeted lint and diff whitespace checks pass. These tests do not verify browser layout, scrolling or visual quality.
- Full audit remains 294 errors and 3 warnings, with no added or removed findings relative to the crypto-guide baseline. Logs: `.preview/local-article-reading-tests.log`, `.preview/local-article-reading-russian-tests.log`, `.preview/local-article-reading-audit.log`. The complete source/release audit and browser visual sign-off remain outstanding; no push or deployment is authorized by this local pass.
- Local HTTP checks return 200 with the new reading markup for the English FundedNext review and both Russian partner reviews. The production build passes compilation (29.8s) and TypeScript (41s), then exits 1 at the unchanged challenge-change sharing-card guard (expected 16/9/5/11; received 21/10/8/13); log `.preview/local-article-reading-build.log`. Do not treat the passing component tests as a full-build or release success. The retained development server remains running at http://127.0.0.1:3214.

### Local follow-up: complete Russian Bright Funded review adaptation

- Adapted the existing long-form partner review into reader-facing Russian: removed internal data-model terminology, payout/fee jargon and the old promotion's suggestion of current availability. Preserved every prior section anchor, primary partner campaign, comparison handoff and the distinction between an affiliate relationship and eligibility. Search description/H1/contents no longer promise a fixed live inventory. Added a primary-source section; kept source links available after price expiry. Sharing description remains unchanged under the Sites approval boundary and still needs an approved count-neutral update before release.
- Re-read five Bright Funded help pages on September 8: evaluation/risk rules, rewards, news, unused-account refunds and country restrictions. Narrow observations and quotes are recorded in `content/data/russian-bright-funded-evidence.json`; no challenge prices, product dates, Trustpilot data or individual country approval were refreshed. The internal 14-day payout-cycle/add-on contradiction remains explicit. The country discussion now includes the source's active-account exception for traders from Pakistan without turning it into permission for a new registration. Product prices remain August 27; this pass is not a complete product recapture.
- Corrected rendering semantics: missing targets no longer mean no evaluation; a missing second-stage target remains visible; unknown prices are not zero inputs to a claimed saving; a missing profit share preserves the known EUR fee but withholds break-even output. Removed unsafe first-tier indexing and arithmetic that assumed one programme's price also described Classic. The lowest-price FAQ is derived from surviving price rows. No fresh prices produces a clear notice and a useful comparison link, while retaining the substantive dated article. The headline product date uses the oldest product capture rather than the newest.
- Help-source freshness is independent of product freshness. Each missing, impossible, future or stale source withholds current FAQ schema without erasing separately fresh EUR prices. Old prices cannot inherit a new help-page date. The July 27 Trustpilot suppression observation now has an explicit stale-status notice and is excluded independently from current FAQ schema, but its dated explanation remains visible; no rating is invented or recaptured.
- Expanded actual-component tests for all five independent help sources, malformed dates, null targets/share/risk/days, explicit zero days, an unpriced/empty product, price expiry, preserved source links, shared cost calculation output, native Russian wording and source-boundary schema. The final expiry run passes all 26 Russian pages plus English home at ten source-derived dates. An initial test callback mistakenly passed Array.every's index as the freshness clock; it was corrected and the final run completed with every assertion enabled. Targeted lint passes. Logs: `.preview/local-bright-review-rendering.log`, `.preview/local-bright-review-audit.log`.
- Actual local HTTP output returns 200 with the new source section and 18 EUR price rows; the sitemap retains September 8 lastmod. Full audit remains exactly 294 errors and 3 warnings, with no added/removed findings. The build passes compilation (39.0s) and TypeScript (27.2s), then exits 1 at the unchanged sharing-card guard (16/9/5/11 expected, 21/10/8/13 received): `.preview/local-bright-review-build.log`. No browser visual verification, complete release, ranking improvement, push or deployment is claimed. FundedNext's corresponding language/null-data pass and the broader source/release backlog remain outstanding.

### Local follow-up: complete Russian FundedNext review adaptation

- Adapted the existing FundedNext review's body, summary, tables, FAQ and comparison links into reader-facing Russian. Removed fixed live-inventory promises from H1/search description/contents, hard-coded summary shares and obsolete cross-page counts. Preserved prior anchors, author, disclosures, all affiliate campaigns, offer eligibility gating and comparison handoffs. Added a dated source section and kept product URLs after price expiry. Sharing description remains unchanged pending separate approval; its old inventory counts are still a release follow-up.
- Re-read five first-party rule pages on September 8 and recorded narrow scope/quotes in `content/data/russian-fundednext-review-evidence.json`: general copying, Instant copying, Instant loss limits, general post-evaluation news treatment, and evaluation-product payout timing/refunds. No programme prices or capture dates, country-access evidence, Instant payout capture or Trustpilot record were refreshed. The review still identifies the dated Russian-resident source conflict rather than claiming access.
- Corrected a material scope conflation: the general copying article and separate Instant article do not impose one universal rule. The review now distinguishes own evaluation-account copying, restrictions involving accounts after evaluation, and same-owner Instant-to-Instant copying; Instant-to-1-Step/2-Step/Lite and different-owner copying remain prohibited in the cited scope. General news wording is explicitly scoped to post-evaluation 1-Step/2-Step/Lite, with the separate Instant guide linked. The 1-Step schedule says five business days, not a calendar-week equivalent. These are corrections to this review, not a full downstream copy-rule audit or a product capture refresh.
- Missing targets no longer mean no target; missing day/payout/share/refund inputs are labelled unknown rather than zero or permission. The explicit absence of an Instant daily cap requires a fresh named source scoped to Instant; another product's null daily cap stays unknown. Missing shares preserve known cash cost but withhold break-even output. Removed a fabricated zero-cap fallback from the Instant example. Existing `challengeTierEconomics`/minimum-cost functions still supply the maths. Headline product dating uses the oldest product capture.
- Product prices and independently checked rules expire separately. Current FAQ schema is withheld when underlying product/rule/country/Instant/EA evidence expires; the old Trustpilot question is independently excluded and its dated section warns when rechecking is needed. Dated long-form explanations and useful source/comparison links remain. Added actual-page fixtures for all five new source dates, impossible/missing/future dates, null targets/shares/limits/payouts/refunds, zero trading days, empty tiers, price expiry, copy-rule scope and the business-day unit. Final rendering suite passes all 26 Russian pages plus English home at ten dates; targeted lint passes.
- Local HTTP output returns 200 with four surviving models, separate copying explanations and the new source section; actual sitemap lastmod remains September 8. Logs: `.preview/local-fundednext-review-rendering-final.log`, `.preview/local-fundednext-review-audit-final.log`, `.preview/local-fundednext-review-build-final.log`. Broader source refreshes, remaining editorial/data audit work, sharing approval and browser visual/interaction verification are still required; no release or SEO outcome is claimed.

### Local follow-up: homepage finder continuity and readable previews

- Inspected the current light homepage implementation after the Godly reference discussion. Preserved the comparison-first silhouette, the two disclosed partners, three selected guides, existing routes and attribution. No article, feature, image, dependency or deployment was added. This is a refinement of the established Light Teal direction, not a claim that visual QA has passed.
- Russian homepage now exposes the same three-stage choice supported by the destination comparison. Both homepage selectors share valid account-size discovery and recover from a removed selected tier or an empty-to-populated data refresh without inventing an account size. The full Russian finder uses the same real-tier fallback when 50K is unavailable. Supported user choices and evaluation-stage requirements remain intact; a true no-match stays a no-match.
- Russian previews identify the product's market and explain that billing models differ, while retaining original currencies, base/monthly/initial-payment labels and alphabetical, one-programme-per-firm ordering. Empty data explains why the dated reviews remain useful. Programme and billing copy is at least 0.875rem; narrow layouts stack fees below names, with wrapping for long labels. These CSS decisions follow the Sites readability guidance, but have not been checked through browser resizing or screenshots.
- Added `npm run test:home-finder`: actual EN/RU component trees with controlled state transitions cover stage selection, comparison-fragment handoff, analytics payload, alphabetical ordering, removed-size recovery, empty/repopulated state, true no-match and market labels. Added actual full-Russian-finder SSR with only a 25K tier to prevent a false empty result. Targeted lint, finder tests, new lifecycle tests and the complete 26-Russian-page/English-home rendering suite at ten dates pass. Logs: `.preview/local-home-finder-expiry-final.log`.
- Local EN and RU homepages both return HTTP 200, with the Russian three-stage control in served output. Build compilation (23.1s) and TypeScript (49s) pass; build exits 1 at the unchanged challenge-change sharing-card guard (expected 16/9/5/11, received 21/10/8/13). Sharing assets/metadata were preserved as required by the Sites explicit-approval boundary. Full audit remains 294 errors and 3 warnings, with exactly no added or removed findings compared with the preceding FundedNext checkpoint. Logs: `.preview/local-home-finder-build.log`, `.preview/local-home-finder-audit.log`. The full local redesign/release objective remains active and unproven; source refreshes, release blockers and explicit browser visual verification remain outstanding. No push or deployment.

### Local follow-up: consistent Russian FundedNext payout timing

- Rechecked FundedNext's official `10701585` reward-schedule article. The existing September 8 narrow evidence remains applicable: Stellar 1-Step uses five business days starting with the first trading cycle after evaluation, not five calendar days or an unconditional calendar-week payout. This is a wording/scope correction, not a price, country-access, Instant payout or product capture refresh. The Instant copying guide was checked for consistency with the corrected review and did not need the proposed copying change.
- Added a client-compatible, separately dated timing helper with exact calendar validation, a 30-day window, firm/product scope, business-day unit and agreement between the saved first-window value and the cited cycle. Missing/conflicting numbers or invalid/expired evidence produce a verification message; unrelated products do not inherit the rule. Applied it to the main review (including its formerly contradictory conclusion cards), MT5 guide, both FundedNext comparisons, payout guide, reviews directory and full Russian finder. Added the actual independent first-party link/date beside downstream tables. Existing affiliate attribution and sharing metadata/assets remain unchanged under the Sites safeguards.
- Russian finder payout sorting now groups conditional windows, ordinary day counts, the business-day programme and unknowns separately. Its control and active-sort explanation no longer imply that five business days are necessarily earlier than another programme's calendar-day window or that a conditional request means immediate receipt. This change concerns the Russian/shared finder sorter; the separate English comparison engine and other outstanding source audits are not certified by this pass.
- Updated editorial dates for the two comparison routes and payout route to September 8; Article dates now use the editorial/date helper where missing, without replacing price-capture dates. Their actual sitemap entries retain the existing URLs and now report September 8. All six affected article routes return HTTP 200 and expose business-day wording in the served Stellar 1-Step payout cells. An initial 45-second HTTP observation timed out while the retained server was busy; its live process subsequently reported completion, and the same server passed the retried checks without a restart.
- Finder unit tests cover date boundaries, missing/conflicting first-window values, wrong firm/product scope and separated payout sorting. Actual rendering fixtures cover all six article payout cells plus the full Russian finder across valid, missing, impossible, expired and future timing dates; the numeric primary-comparison FAQ schema is withheld if its timing source is invalid. Full rendering passes 26 Russian pages plus English home at ten dates, and targeted lint passes. Log: `.preview/local-russian-payout-expiry-final.log`.
- Final build passes compilation (64s) and TypeScript (83s), then exits 1 at the unchanged challenge-change sharing-card guard (expected 16/9/5/11, received 21/10/8/13). Full audit remains 294 errors and 3 warnings with no added/removed findings. Logs: `.preview/local-russian-payout-build.log`, `.preview/local-russian-payout-audit.log`. Broader source/editorial corrections, the sharing approval and explicit browser verification remain outstanding. Full objective remains active; nothing pushed or deployed.

### Local follow-up: complete Russian primary-partner comparison adaptation

- Reworked the existing FundedNext/Bright Funded comparison as a substantive Russian article, preserving its URL, prior anchor, product/section markers, author attribution and affiliate campaigns. Removed mixed editorial/programming jargon, fixed live-inventory promises from the normal search description and unsupported shortcuts such as a fixed nine-day advantage, one firm's values describing both firms, and a blanket platform-based winner. Retained separate one-step, two-step, Instant, cost, payout, identity, country, trust and decision sections. This is an existing-page improvement, not a new article or claim of SEO performance.
- Re-read Bright Funded's platform, evaluation-rule and reward pages. Added a narrow September 8 platform observation to `russian-bright-funded-evidence.json`: its official help page lists MT5, DXTrade and cTrader, not the comparison's old TradeLocker claim. Corrected the comparison's platform logic and linked the official source without changing challenge prices/capture dates or asserting individual country access. This additional help-source record also participates in the existing Bright review's independent source checks. The unresolved next-cycle/add-on contradiction is now explicit both in payout rows and in the detailed payout section.
- Each programme's targets, loss caps and days render independently; missing targets no longer become no evaluation or silently omit a later phase. Unknown shares retain the known USD/EUR expense while withholding break-even output, using the existing cost helpers; the cost table names the corresponding account size. Invalid/unpriced tiers cannot become zero-cost offers. Expired prices leave a meaningful empty-table explanation and the long-form analysis with all original programme-source URLs. The page uses hourly regeneration and the oldest supporting programme date, not the newest.
- Current FAQ schema requires the named programme pairs, usable price rows, both firms' payment/KYC records and independently fresh rule/process/market and FundedNext timing checks. Dated Trustpilot observations are labelled separately as requiring rechecking; suppression is not represented as a zero rating. Added a source section, contents links with actual targets and five named keyboard-scroll table regions, extending the existing light reading system under the Sites skill. Preserved sharing description/assets exactly; the old inventory-count sharing text remains a separate approval/release follow-up.
- Tests cover actual rendered comparison rows, cost output, unknown and partial targets, null shares, zero trading days, empty tiers, all-price expiry, independent rule/KYC/payment dates, native wording, platform correction, payout ambiguity, five table regions and all contents anchors. Full rendering passes all 26 Russian pages plus English home at ten dates; targeted lint passes. The initial build exposed an optional-day TypeScript mismatch, corrected with a null fallback (not zero). Final build passes compilation (30.2s) and TypeScript (34.8s), then exits 1 only at the existing sharing-card guard (16/9/5/11 expected; 21/10/8/13 received). Logs: `.preview/local-primary-comparison-rendering-verified.log`, `.preview/local-primary-comparison-build-final.log`.
- Actual local comparison returns HTTP 200 with corrected platforms, source section and about 2,711 token-like words in the article extraction; this count is a size check, not editorial quality or uniqueness proof. The existing sitemap route retains September 8 lastmod. Final audit remains 294 errors and 3 warnings with no added/removed findings: `.preview/local-primary-comparison-audit-final.log`. No browser visual sign-off, complete source recapture, full release, push or deployment. Broader editorial/data work and release approvals remain; the full goal is active and unproven.

### Local follow-up: Russian freshness labels and finder handoffs (2026-09-14)

- Added the shared source-age notice to the Era Trade, KasCapital, PropLive, Russian local-company overview and Russian reviews guide. Local research now exposes its dated evidence as a separate status, while product freshness remains independently gated; no local operator was promoted to an active affiliate route.
- Routed local-review and challenge-guide comparison buttons directly to `/ru/luchshie-prop-firmy#podbor`, and added the same neutral selector handoff to the reviews guide. The Russian site now moves from explanation to a specific product selector without adding a mass of new articles or mixing local and global models.
- Replaced the stale 19-firm/89-product claim in the existing English definition article with the dated 2026-09-14 snapshot (16 firms, 66 current products), and made the cheapest landing count data-derived. Audit fixtures for swing/instant/overall/cheapest inventories now use current data-driven safeguards instead of obsolete literals.
- Final local build passes 509/509 static pages and TypeScript. Light release check passes all 279 sitemap pages; release crawl reports 279 sitemap URLs, 998 internal targets, 611 JSON-LD blocks, 333 images and 0 errors. Article-content tests and lint pass. Audit remains non-zero only because FXIFY, OFP Funding and The Funded Trader have stale or conflicting first-party data and dependent comparisons are withheld; no dates were fabricated. Local preview remains at `http://127.0.0.1:3215`. No commit, push or deployment was made.

### Local follow-up: stale-source queue and Russian access boundary (2026-09-14)

- Added `content/data/ofp-funding-plan-conflict-evidence.json` and an open `source-conflict` watch. OFP Funding's current selector shows a selected 50K Instant Lite configuration, while its first-party terms describe several model families without a complete current tier-to-price map. The 2026-07-27 structured prices remain dated material and are not refreshed.
- Added `content/data/the-funded-trader-lineup-conflict-evidence.json` and an open `source-conflict` watch. The current homepage/configurator presents a refreshed Knight/Royal/Dragon lineup and package-level values without a named-programme ladder; the current homepage also lists Russia as restricted.
- Added the dated The Funded Trader access finding to `content/data/russian-market-evidence.json`. It is an access boundary only and does not make the stale product capture current.
- Updated the challenge-change social-card consistency constants to 30 ledger entries, 16 affected firms, 14 verified changes and 16 open watches.

Validation: `npm run test:watch-evidence`, `npm run test:russian-expiry`, and `npm run audit -- --warn` (the audit remains blocked by the existing FXIFY/OFP Funding/The Funded Trader stale product captures and their dependent neutral-comparison checks; no dates were fabricated).

No commit, push, or deployment was made.

### Local follow-up: partial FXIFY Lite recapture and language/sitemap gate (2026-09-15)

- Re-read FXIFY's first-party Instant Funding Lite launch article and merged the six published tiers ($2,500–$100,000 at $19/$39/$79/$149/$249/$399) with a 2026-09-15 product capture. The remaining FXIFY products retain their older dates because the current programme cards do not expose a complete, consistent tier ladder; the price conflict remains visible in the watch evidence.
- Rechecked OFP Funding's official selector and terms today. The current selected 50K Lite price is still observable, but the pages do not expose a complete model-to-tier checkout ladder; the conflict watch is dated 2026-09-15 while all unverified OFP product captures remain dated 2026-07-27.
- The Russian no-challenge guide now includes the two fresh FXIFY phase-0 products alongside the existing partner and non-partner records: 11 products across 9 firms. Its sitemap entry now uses the actual latest supporting capture (2026-09-15), and the release-crawl regression constants were updated to match the data-derived result.
- Added a pre-hydration document-language sync so `/ru` routes set the root document language to `ru` while English routes remain `en`; Russian content still carries its server-rendered `lang="ru"` boundary for non-script clients. The Russian release check now guards this marker.
- Scanned the served Russian text for internal English jargon and translated the remaining diaspora-table `checkout` label to reader-facing Russian (`оформление покупки`). No flagged `checkout`, `affiliate`, `CTA`, allocation, tier, or internal implementation terms remain in that route's visible text.
- Final local verification: `npm run build` generated 509/509 pages; `npm run lint`, `npm run test:russian-expiry`, `npm run test:watch-evidence`, `npm run test:article-content`, `npm run check:finder-release`, `npm run check:light-release`, `npm run audit:alternatives`, and `npm run crawl:release:strict` pass. The crawl reports 279 sitemap URLs, 1,007 internal targets, 611 JSON-LD blocks, 342 images and 0 errors.
- The editorial audit remains intentionally non-zero at 84 errors: OFP Funding and The Funded Trader still need complete first-party recaptures, and the older FXIFY products plus dependent comparison/UK/India evidence remain held. No dates were fabricated. Preview remains at `http://127.0.0.1:3214`; no commit, push or deployment was made.

### Local follow-up: Search Console intent pass for the Russian ranking page (2026-09-15)

- The 2026-09-07 Search Console export is filtered to `/ru/luchshie-prop-firmy` for the last seven days. It records 161 impressions, 0 clicks and an average position of 56.94. The leading queries are `проп компании россии` (20 impressions), `проп трейдинговые компании россии` (19), `список проп компаний` (18) and `проп фирмы с мгновенным финансированием` (9). The country split includes Russia (52 impressions), Kazakhstan (38), Ukraine (36) and Belarus (31), supporting a diaspora/global page rather than a Russia-only access promise.
- Updated the page search title to `Рейтинг проп-компаний 2026: Россия и глобальные фирмы` and the 147-character description to name the list, firms in Russia and abroad, and programmes without a challenge. The visible H1 stays focused on Russian-speaking traders. Added one factual above-the-fold sentence explaining that instant-funding/no-challenge products are included and stale source data is held out of the current ranking.
- This is a single existing-page intent/CTR test. It does not add articles, change rankings, assert firm access for Russian residents or alter affiliate sorting. The release-crawl expectation was updated to keep the approved title explicit.
- Local checks after rebuild: `npm run build` (509/509), `npm run lint`, `npm run test:russian-expiry`, `npm run test:article-content`, `npm run check:finder-release`, and `npm run crawl:release:strict` pass. The crawl reports 279 sitemap URLs, 1,007 internal targets, 611 JSON-LD blocks, 342 images and 0 errors. The served page returns the new title/description and the root-language sync marker. No commit, push or deployment was made.

### Local follow-up: OFP selector recheck and article provenance (2026-09-15)

- Rechecked OFP Funding's public homepage selector in a browser across all nine visible model tabs and every displayed base-price size. The observed ladders match the 2026-07-27 structured capture, including the two Classic variants, Lite, two Plus variants, Prime, Pro, One Phase and Two Phase. The selected default Classic configuration rendered a 20% Consistency Ratio and the other visible tabs rendered `No`.
- The current first-party Terms Schedule A still conflicts with that selector: it lists different model parameters (including Lite's leverage and the Classic consistency value) and no complete price ladder. Cited the public Terms page directly in the evidence and watch, updated `content/data/ofp-funding-plan-conflict-evidence.json` with the full observation, and kept the `source-conflict` watch open; no OFP product date was refreshed and no checkout quote is implied.
- Updated the existing OFP review to explain the current-vs-structured provenance boundary and the conflicting consistency sources. This keeps the dated table useful while preventing the recheck from becoming an unsupported “current” claim.
- `npm run test:article-content` and `npm run test:watch-evidence` pass. The final local `npm run build` generated 509/509 pages; `npm run crawl:release:strict` reports 279 sitemap URLs and 0 errors, and `npm run check:light-release` passes all 279 HTTP checks. Scoped OFP audit still fails on the deliberate 2026-07-27 freshness hold and dependent neutral-comparison evidence; FXIFY and The Funded Trader remain separate stale-source blockers. No commit, push or deployment was made.

### Local follow-up: FXIFY checkout evidence (2026-09-15)

- Rechecked the official FXIFY purchase flow in read-only mode and recorded named selectors for all 8 product families. The structured data now contains 54 current list-price tiers; Instant Funding Lite uses the checkout's five-tier ladder (`$19/$44/$85/$169/$289`) rather than the older six-tier launch article.
- The Russian no-challenge service now renders 12 fresh phase-0 products across 9 firms, including FXIFY's current Standard and Lite paths. It still distinguishes list price from coupon/add-on totals and does not infer Russian eligibility from a global checkout page; FXIFY's published restricted-country text remains a separate access boundary.
- The old program-page `$250/$5K` card is documented as a presentation conflict in the evidence ledger. It is not used to overwrite checkout tiers, and no ranking gain or conversion result is claimed from this recapture.
- The FXIFY review, challenge watch and downstream lifecycle/consistency/overtrading/profitability rows now share the 2026-09-15 capture date. No deployment, commit or push was made.
